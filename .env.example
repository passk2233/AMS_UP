# Copy this file to `.env` and fill in the values. `.env` is gitignored.
#
# Base URL of the AMS backend REST API, including scheme and any path prefix.
# Examples:
#   API_URL=http://10.0.2.2:8080/api        (Android emulator -> local backend)
#   API_URL=https://abc123.ngrok-free.app/api
#
# For CI / release builds on a clean checkout (where this .env is absent, since
# it's gitignored) you can bake the URL in at build time instead:
#   flutter build appbundle --release --dart-define=API_URL=https://api.phetsamone.xyz/api/v1
API_URL=
